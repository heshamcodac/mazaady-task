/** @type {import('next').NextConfig} */
const nextConfig = {
  //   webpack: (config) => {
  //     config.resolve.alias["@components"] = path.join(
  //       __dirname,
  //       "./app/components"
  //     );
  //     return config;
  //   },
};

module.exports = nextConfig;
